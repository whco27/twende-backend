# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# Database Configuration
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/twende_tours

# Frontend Configuration
# Set ENABLE_CORS=false when frontend is served from same origin (integrated deployment)
ENABLE_CORS=true
# Frontend URLs for CORS (comma-separated for multiple origins) - only used when ENABLE_CORS=true
FRONTEND_URL=http://localhost:5173

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Email Configuration for Notifications
# These settings are required to enable email notifications
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=noreply@twendetours.com

# Admin emails for new user registration notifications (comma-separated)
ADMIN_EMAILS=admin@twendetours.com

# M-Pesa Daraja API Configuration
# Get these from the Safaricom Daraja Developer Portal: https://developer.safaricom.co.ke/
DARAJA_ENV=sandbox
DARAJA_CONSUMER_KEY=your_consumer_key
DARAJA_CONSUMER_SECRET=your_consumer_secret
DARAJA_PASSKEY=your_passkey
DARAJA_SHORTCODE=174379
DARAJA_CALLBACK_URL=https://your-backend-url.com/api/payments/callback